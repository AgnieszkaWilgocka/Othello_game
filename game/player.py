# class Player:
#     def __init__(self, name):
#         self.name = name
#         self.field = []
#         self.score = 0
#         # self.current_player = 1
#
#     def move(self, x, y, second_player):
#         global current_player
#         if [x, y] in self.field:
#             # self.field.append([int(x), int(y)])
#             current_player *= (-1)
#         else:
#             print('Tu nie mozna stanac')
        # second_player.current_player *= (-1)
        #
        #
        #
        # temporary_list = []
        # if x and y != self.field and x and y != second_player:
        #     temporary_list.append(x)
        #     temporary_list.append(y)
        #     for el in temporary_list:
        #         self.field.append(el)
        # if (x, y) == self.field or (x, y) == second_player:
        #     print('Nie mozesz tu stanac')
